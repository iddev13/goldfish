import { FC } from "react";
import './CatalogNav.scss'

const Catalog: FC = () => {
	return (
		<div className="catalogNav">
			<button className="catalogNav__btn">каталог</button>
		</div>
	)
}

export default Catalog